paths:
  root_dir: ${oc.env:PROJECT_ROOT}
  log_dir: ${paths.root_dir}/logs/${experiment.project}/${experiment.name}
  logger_dir: ${paths.root_dir}/logs
  ckpt_dir: ${paths.log_dir}/ckpt
  inference_dir: ${paths.log_dir}/inference
  # 直接使用正确的数据路径（如果环境变量 DATA_DIR 未设置或需要覆盖，使用此路径）
  data_dir: /ibex/project/c2318/material_discovery/clean_data/preprocessed_data/MOF-DB-1.1
  # 如果需要使用环境变量，取消下面的注释并注释掉上面的行
  # data_dir: ${oc.env:DATA_DIR,/ibex/project/c2318/material_discovery/clean_data/preprocessed_data/MOF-DB-1.1}
  # data_dir: /ibex/project/c2318/material_discovery/MOFFLOW2_data
  # data_dir: /home/suny0a/chem_root/MOFFlow-2/MOFFLOW2_data